import useRequestUser from "../hooks/useRequestUser";


const NotesManager = () => {

// 	const logData=useRequestUser('login')
// 	if(logData.isUserLogged()) {
// 		return (
// 			<p>
// Landing page
// 			</p>
// 		)
// 	}
	return  (
		<>

		</>
	);

}

export default NotesManager;
